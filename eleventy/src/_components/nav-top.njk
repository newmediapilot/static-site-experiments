<!-- begin nav-top.njk -->
<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                wut
                {{
                    page | dump
                }}
                <a class="nav-link {%  if item.url == page.url %}active{%  endif  %}" href="{{ item.url }}">item1</a>
                <a class="nav-link {%  if item.url == page.url %}active{%  endif  %}" href="{{ item.url }}">item2</a>
                <a class="nav-link {%  if item.url == page.url %}active{%  endif  %}" href="{{ item.url }}">item3</a>
            </li>
        </ul>
    </div>
</nav>
<!-- end nav-top.njk -->